'use strict';

describe('Directive: rdSearchForm', function () {
  beforeEach(module('angularjsRundownApp'));

  var element;

  // it('should make hidden element visible', inject(function ($rootScope, $compile) {
  //   element = angular.element('<rd-search-form></rd-search-form>');
  //   element = $compile(element)($rootScope);
  //   expect(element.text()).toBe('this is the rdSearchForm directive');
  // }));
});
